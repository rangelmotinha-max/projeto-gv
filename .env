# Porta do servidor (padrão 3000)
PORT=3001

# Configurações de conexão com o banco MySQL
# Ajuste o DB_PASSWORD se o usuário root tiver senha definida no XAMPP
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=sgv
